# Node.js
node_modules/
dist/
*.log

# Env / secrets
.env
.env.local

# OS
.DS_Store
Thumbs.db

# Editor
.vscode/
.idea/

# Tool runtime
.codenomad/

# ВАЖНО: data/ НЕ игнорируется — состояние контента коммитится обратно в репозиторий,
# чтобы GitHub Actions (stateless) сохранял записи и offset апрувов между запусками.
# data/ содержит только посты и служебные файлы (без секретов).
